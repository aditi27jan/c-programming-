#include <stdio.h>
int main()
{
    int length, breadth;
    printf("enter length and breadth: ");
    scanf("%d %d", &length, &breadth);
    printf("area=%d\n", length*breadth);
    printf("perimeter=%d\n", 2*(length+breadth));
    return 0;
}
