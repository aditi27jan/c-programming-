# include <stdio.h>
int main()
{
 int n,sum=0,original;
 printf("enter number");
 scanf("%d",&n);
 original=n;
 while (n!=0)
 {
   int d=n%10;
   sum=sum+(d*d*d);
   n=n/10;
 }
 if (sum==original){
     printf("armstrong");
 }else{ 
       printf("not a armstrong");
 }
 return 0;
}
