#include <stdio.h>
int main()
{
    float radius;
    printf("enter the radius: ");
    scanf("%f", &radius);
    printf("area=%f\n", 3.14 * radius * radius);
    printf("circumference=%f\n", 2 * 3.14 * radius);
    return 0;
}
